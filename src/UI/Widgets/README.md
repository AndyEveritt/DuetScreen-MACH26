# Widgets

- A widget is a reusable UI element that has its own presenter and can handle interaction logic.
- Widgets are typically more complex than components and can manage their own state and behavior.