# Components

- A component is a reusable UI element.
- They do not have a presenter and any interaction logic outside of the components core functionality should be handled by callbacks.
    - ie: a slider component should have logic to handle the slider movement, but not to handle what happens when the slider value changes.
